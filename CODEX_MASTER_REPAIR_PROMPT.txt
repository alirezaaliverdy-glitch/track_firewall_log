TASK: Runtime, Router, Data and Route Contract Convergence

Read AGENTS.md and current source, but treat archived Task 18/19/20 documents as historical only.
Do not begin feature development until Phase 0-3 pass.

NON-NEGOTIABLE SAFETY
- Do not reset, truncate, seed, db push, or delete the database.
- Do not create a temporary PostgreSQL cluster.
- Do not rewrite the database name or port in code.
- Do not print or commit secrets.
- Do not use Docker for the local Windows runtime.
- Do not claim success from HTTP 200 alone.
- Do not claim a route works unless its unique page identity and data contract are verified.

PHASE 0 - SNAPSHOT IDENTITY
1. Report current git HEAD and tracked/untracked status.
2. Report active listeners for 4000, 5173, 5174 and identify processes.
3. Stop duplicate frontend/backend processes.
4. Read the normal runtime datasource without printing the password and report host, port, database, schema and current_user.
5. Confirm the existing database containing the user's historical Device, Asset, Credential, Finding and ActionPlan rows.
6. Create one database dump before schema reconciliation.
7. Stop if the historical database cannot be identified.

PHASE 1 - ONE NORMAL RUNTIME
1. Normal `cd backend && npm run dev` must use exactly one DATABASE_URL from backend/.env.
2. Keep the existing database name and credentials; local PostgreSQL is on 127.0.0.1:5432 unless read-only discovery proves otherwise.
3. Remove all DB-name/port rewrite logic and temporary-runtime fallbacks.
4. Ensure PrismaPgAdapter and pg.Pool consume the exact same resolved URL.
5. Pin frontend to port 5173 using strictPort.
6. Use the Vite relative proxy path for local API calls; do not hardcode http://localhost:4000 in the browser.
7. Add redacted runtime identity to /api/health/ready: host, port, database, schemaReady.
8. Prove 10 consecutive readiness checks and login/auth-me.

PHASE 2 - ROUTER CONVERGENCE
1. Replace the hand-written pathname matcher with react-router-dom.
2. Replace all direct history.pushState and window.location.pathname navigation with Link/useNavigate.
3. Add a real 404 page. Unknown routes must never render Dashboard.
4. Preserve exact action result and guided action deep links.
5. Add route identity markers for E2E assertions.
6. Build one shared feature/route manifest so app routes, navigation and product-state cannot drift.
7. Add a parity test covering all 53 current feature keys.

PHASE 3 - DATA CONVERGENCE
1. Audit Device and Asset tables read-only.
2. Produce a report of:
   - Device without Asset
   - Asset without Device
   - Linux Device excluded from monitoring
   - missing observability tables/migrations
3. Define Device as connection/control entity and Asset as its inventory projection.
4. Make Device + Asset creation/update atomic in a Prisma transaction.
5. Add an idempotent repair command for unambiguous orphan links; do not auto-change ambiguous rows.
6. Reconcile Prisma migration history safely against the existing schema.
7. Add schemaReady and model row-count smoke checks.
8. Prove /api/assets, /api/devices and monitoring counts are consistent.

PHASE 4 - ONBOARDING CONVERGENCE
1. Persist onboarding sessions in PostgreSQL instead of an in-memory Map.
2. Change the misleading "Save information" action into "Save step and continue".
3. Add one primary state-aware Continue button for:
   answers -> test-connection -> detect-platform -> discover -> build-preview -> commit.
4. Keep advanced step buttons available for debugging, not as the normal required UX.
5. Commit Device, Asset, location/site references, initial health and audit record atomically.
6. Show an animated success toast only after persisted Device and Asset are verified.
7. Redirect to /assets/devices/:deviceId and render the real workspace.
8. Add a full onboarding E2E test with an approved controlled target or deterministic connector fixture.

PHASE 5 - REAL ROUTE CONTRACTS
Use ROUTE_CONTRACT_MATRIX.csv as the source list.
For every route choose exactly one:
A. implement a distinct working page contract;
B. redirect to the canonical route;
C. remove it from user-visible navigation until implemented.

At minimum fix these aliases:
- /actions/pending, /actions/history, /actions/:actionId
- /monitoring/devices, /monitoring/devices/:deviceId, /monitoring/daily-check
- /assets/vendors/cisco/devices
- /tools/traceroute, /tools/ip-info, /tools/subnet, /tools/monitors
- /settings

Do not display PlannedState from an operational dashboard button.
Do not mark Nmap planned when the worker/API exists; derive status from a runtime capability probe.

PHASE 6 - REPEATABLE ACCEPTANCE
1. Add @playwright/test to the repository.
2. Test every route in Persian and English at 1440x900 and 390x844.
3. Assert unique page identity, RTL/LTR, no overflow, no unexpected 4xx/5xx, and functional primary controls.
4. Use an isolated test database/schema for backend suites so parallel tests cannot race.
5. Add one command `npm run test:all` that runs:
   lint, builds, backend tests, migration test, route parity, locale, UTF-8 and Playwright.
6. Do not commit a milestone unless test:all is clean.

PHASE 7 - PROJECT HYGIENE
1. Rotate any secrets that were included in review ZIPs.
2. Ensure .env, dist, screenshots, .runtime, .playwright-mcp and generated evidence are excluded from review archives.
3. Move historical task documents to docs/archive.
4. Keep CODEX_HANDOFF.md under 100 lines and describe only the current runtime.

COMMIT BOUNDARIES
Create separate commits only after each phase passes:
- fix: converge normal runtime
- refactor: replace manual router
- fix: converge device asset data
- fix: make onboarding atomic
- feat: implement route contracts
- test: add full product acceptance
- chore: clean project memory and artifacts

FINAL REPORT MUST INCLUDE
- exact root causes
- exact files changed
- database identity without secrets
- Device/Asset reconciliation counts
- route contract status for all 53 routes
- commands and test results
- remaining honest blockers
