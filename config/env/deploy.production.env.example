# Deployment variables consumed by docker-compose.production.yml.
# Use an immutable version tag. Do not use latest.
DEPLOY_VERSION=v0.0.0
FRONTEND_IMAGE=ghcr.io/owner/repository/frontend:v0.0.0
BACKEND_IMAGE=ghcr.io/owner/repository/backend:v0.0.0
FIREWALL_HTTP_PORT=80
