import type { CommandCatalogEntry } from "./types.js";

// Reserved for the next connector phase. Empty means no Cisco request can be executed.
export const CISCO_COMMAND_CATALOG = Object.freeze([] as CommandCatalogEntry[]);

