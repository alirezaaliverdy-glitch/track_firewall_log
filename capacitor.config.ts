const config = {
  appId: "com.firewallsoar.app",
  appName: "Firewall SOAR",
  webDir: "dist",
  server: {
    androidScheme: "https"
  },
  plugins: {
    SplashScreen: {
      launchAutoHide: true
    }
  }
};

export default config;
