export const theme = {
  radius: {
    sm: "var(--radius-sm)",
    md: "var(--radius-md)",
    lg: "var(--radius-lg)"
  },
  color: {
    background: "var(--app-bg)",
    surface: "var(--app-panel)",
    border: "var(--app-border)",
    text: "var(--app-text)",
    muted: "var(--app-muted)",
    primary: "var(--app-accent)"
  }
} as const;
