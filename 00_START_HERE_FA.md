# بسته تسک تثبیت، هوش مصنوعی، امنیت و آمادگی موبایل

این پوشه را در ریشه پروژه قرار بده. سپس متن `12_CODEX_START_PROMPT.txt` را در Codex اجرا کن.

## هدف
این بسته پروژه را از صفر بازنویسی نمی‌کند. بخش‌های سالم حفظ می‌شوند و کار به صورت فازبندی‌شده انجام می‌شود:

1. ثبت baseline و قفل‌کردن رفتار فعلی
2. RBAC، Rate Limit و CSRF
3. تفکیک درست چت عادی، سؤال دستگاه و درخواست اکشن
4. اجرای اکشن‌های جدید AI از مسیر کنترل‌شده Backend و Connector
5. Refactor فایل‌های بزرگ بدون تغییر رفتار
6. آماده‌سازی Production و پاکسازی مستندات/باندل
7. آماده‌سازی PWA و زیرساخت موبایل
8. تست نهایی با Playwright و تست‌های Backend

## نکته مهم درباره «دستور» و «پرامپت»
برای جلوگیری از اشتباه، الگوی قطعی اکشن به شکل زیر تعریف می‌شود:

- `دستور: ...`
- `پرامپت: ...`
- `پرامت: ...`
- `command: ...`
- `prompt: ...`
- حالت دستی `Action` در UI

وقتی یکی از این Markerها به عنوان پیشوند استفاده شود، پیام قطعاً `action_request` است. استفاده معمولی از واژه «دستور» داخل یک سؤال توضیحی نباید تصادفی اکشن بسازد.

## قواعد اجرا
- هر فاز Commit جدا داشته باشد.
- اگر تست فاز شکست خورد، فاز بعد شروع نشود.
- فایل‌های `.env`، Credential، Runtime DB و Backup وارد Commit نشوند.
- تغییرات نامرتبط موجود در Worktree لمس یا Stage نشوند.
- اجرای مستقیم Command از Frontend یا AI ممنوع است؛ فقط Backend Connector اجرا می‌کند.
