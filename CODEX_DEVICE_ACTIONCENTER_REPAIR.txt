Read this file completely and execute it as the authoritative task.

PHASE: Device Workspace + Action Center UX and Runtime Convergence

Goal:
Make the registered-device workspace and Action Center fully operational, user-friendly, and backed by real backend behavior.

Rules:
- Continue from the current worktree.
- Do not redo previous onboarding phases.
- Preserve all existing Device, Asset, Credential, Finding, ActionPlan, and migration data.
- Do not reset, seed, recreate, restore, or destructively migrate the database.
- Do not use Docker.
- Do not expose secrets.
- Do not create fake success states.
- Never mark success unless connectorInvoked=true.

Current confirmed problems:
1. Registered devices can remain unverified with no clear workflow to test connection, select credentials, retry, detect platform, discover capabilities, or verify.
2. Action Center is incomplete: buttons are missing, layouts are clipped, controls disappear, and some actions cannot execute.
3. Frontend and backend behavior must be validated together against the real runtime.

First reproduce all issues with authenticated Playwright and the real backend. Record exact routes, requests, status codes, hidden controls, overflow, and dead buttons before editing.

DEVICE WORKSPACE

Add a prominent “Connection & Verification” section to every device workspace.

Show:
- verification status
- vendor and platform
- host/IP and port
- connection method
- credential reference
- last attempt
- last success
- connectorInvoked
- sanitized error

Controls:
- Select or replace credential
- Test connection
- Retry
- Detect platform
- Discover capabilities
- Verify device
- Start new verification session
- View connection history

Backend requirements:
- Reuse existing onboarding connector and credential-validation services.
- Add explicit endpoints for an existing registered device:
  POST /api/devices/:deviceId/connection-test
  GET  /api/devices/:deviceId/verification
  POST /api/devices/:deviceId/verification/retry
  POST /api/devices/:deviceId/verification/commit
- Persist attempts, timestamps, sanitized errors, and verification state.
- Prevent duplicate concurrent attempts.
- An unreachable device stays registered but unverified.
- Promote to verified only after a real successful connector invocation.
- Unsupported vendor/method pairs must be rejected before execution.
- Never expose credential secrets.

ACTION CENTER

Redesign around this lifecycle:
Draft → Needs input → Ready for confirmation → Confirmed → Executing → Succeeded / Failed / Cancelled

Required UI:
- Summary cards
- Search and filters
- Action list/table
- Action details panel
- Persistent execution controls
- No hidden primary buttons
- No horizontal overflow

Required controls:
- Create ActionPlan
- Review
- Edit parameters
- Select device
- Select credential when required
- Preview commands
- Confirm
- Execute
- Retry
- Cancel
- View evidence
- View connector result
- Open related device workspace

Execution rules:
- Unsupported actions must not expose an enabled Execute button.
- No action is successful unless connectorInvoked=true.
- Show command preview, backend response, evidence, connector result, and sanitized errors.
- Preserve deep links:
  /actions
  /actions/pending
  /actions/history
  /actions/:actionId
  /assets/devices/:deviceId
- Use React Router only.
- Preserve Persian RTL and English LTR.
- Add loading, empty, error, unauthorized, and not-found states.
- Verify layouts at 1366x768, 1440x900, and 1920x1080.

Acceptance:
1. Existing Cisco device workspace opens correctly.
2. Credential can be selected without exposing secrets.
3. Test Connection invokes the real connector.
4. connectorInvoked=true on a real attempt.
5. Unreachable devices remain unverified.
6. Retry works.
7. Successful connection promotes to verified.
8. Refresh and backend restart preserve verification state.
9. Action Center exposes all primary controls.
10. An ActionPlan moves through draft → confirm → execute.
11. Unsupported actions remain non-executable.
12. Failed actions show sanitized evidence.
13. Deep links work directly.
14. No missing controls or horizontal overflow at required resolutions.
15. Existing database counts remain unchanged except temporary acceptance records, which must be cleaned up.

Validation:
- backend build
- frontend build
- all backend tests
- all frontend tests
- route-contract tests
- authenticated Playwright acceptance
- git diff --check
- prisma migrate status
- database before/after counts

Commit separately:
1. device verification backend
2. device workspace UI
3. Action Center backend contract
4. Action Center UI redesign
5. acceptance tests and cleanup

At the end report:
- root causes
- files changed
- endpoints added or changed
- test results
- browser evidence
- database before/after counts
- commit hashes
- remaining honest blockers
